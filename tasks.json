{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Bazel Build GC Tests",
            "type": "shell",
            "command": "bazel build -c dbg //test/cortecs/gc",
            "group": "build"
        }
    ]
}

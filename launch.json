{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "GC Tests",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bazel-bin/test/cortecs/gc/gc",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "Bazel Build GC Tests",
            "sourceMap": {
                "/proc/self/cwd": "${workspaceFolder}",
            },
        }
    ]
}
